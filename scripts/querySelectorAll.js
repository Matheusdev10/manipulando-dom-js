const element = document.querySelectorAll('#one');
//console.log(element);

const elements = document.querySelectorAll('#one');

elements.forEach((elem) => console.log(el));
console.log(element);

//html colection nao aceita
//for each e no queryselectorall aceita

//getElementById (element)

//getElementByClassName (HtmlCollection/
//nao consigo fazer forEach)

//getElementByTagName (HtmlCollection)

//querySelector (element)

//querySelectorAll (NodeList/ da pra fazer forEach)
